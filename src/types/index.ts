export interface TokenType {
    name: string;
    address: `0x${string}`;
    type: string;
}

export interface TOKEN {
    name: string;
    address: `0x${string}`;
    symbol: string;
    decimals: number;
}
